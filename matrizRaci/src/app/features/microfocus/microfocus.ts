import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-microfocus',
  imports: [RouterLink],
  templateUrl: './microfocus.html',
  styleUrl: './microfocus.scss',
})
export class Microfocus {}
